from tkinter import Menu, Label, Frame

def show_habits(frame):
    # Clear the content frame and display the list of books
    label = Label(frame, text="habits", font=("Arial", 14))
    label.pack(pady=20)